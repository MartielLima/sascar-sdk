# F5 — Documentação e DX — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Atualizar a documentação do `sascar-sdk` para refletir o estado real pós-auditoria (63 métodos, erros tipados, SascarClientOptions, exemplos de timeout/retry), adicionar CHANGELOG e `.env.example`, corrigir credenciais placeholder do README.

**Architecture:** Edições cirúrgicas no README.md existente (que já tem 2.804 linhas), adição de CHANGELOG.md, adição de .env.example. Sem mudanças em código TypeScript.

**Tech Stack:** Markdown.

**Reference:** Master design F5.

**Estado atual:**
- README tem 2.804 linhas mas está desatualizado (não menciona 3 novos métodos da F2, nem SascarClientOptions da F3).
- Sem CHANGELOG.
- Sem .env.example.
- README exemplo de uso tem `usuario: "foo"`, `senha: "bar"` — placeholder ruim.

---

## Tarefas

### Task 1: Criar `.env.example`

**Files:**
- Create: `.env.example`

- [ ] **Step 1: Escrever `.env.example`**

Conteúdo:
```bash
# Credenciais de acesso ao WebService SasIntegra da Sascar/Michelin.
# Obtenha suas credenciais com o suporte da Sascar:
#   https://connectedfleet.michelin.com/
SASCAR_USUARIO=seu_usuario_aqui
SASCAR_SENHA=sua_senha_aqui
```

- [ ] **Step 2: Commit**

```bash
git add .env.example
git commit -m "docs: add .env.example with credential placeholders"
```

---

### Task 2: Criar CHANGELOG.md

**Files:**
- Create: `CHANGELOG.md`

- [ ] **Step 1: Escrever `CHANGELOG.md`**

Conteúdo:
```markdown
# Changelog

Todas as mudanças notáveis neste projeto são documentadas aqui.
O formato segue [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/),
e este projeto adere ao [Semantic Versioning](https://semver.org/lang/pt-BR/).

## [Não lançado]

### Adicionado

- 3 novos métodos para cobertura 100% do manual SasIntegra v2.07:
  - `obterEnderecoPosicao(latitude, longitude)` — reverse geocoding
  - `obterPacotePosicoesRFNacional(quantidade)` — posições de rastreadores de cargas
  - `obterPacotePosicaoMotoristaHistorico(dataInicio, dataFinal, idVeiculo?)` — histórico com info de motorista
- 2 novos erros tipados: `SascarTimeoutError` e `SascarAuthError`.
- Timeout configurável via `AbortController` (default 30s).
- Retry automático com backoff exponencial (250ms→500ms→1s ±20% jitter) para 5xx e erros de rede.
- Segundo parâmetro opcional no construtor: `SascarClientOptions { timeoutMs, maxRetries, wsdlUrl }`.
- Camada de transporte isolada em `src/transport/`: `envelope.ts`, `fault.ts`, `http.ts`.
- 4 classes de erro tipadas: `SascarApiError`, `SascarConnectionError`, `SascarRateLimitError`, `SascarTimeoutError`, `SascarAuthError`.
- `SascarApiError` agora carrega `fault?: SascarSoapFault`.
- Testes de integração com `nock` para todos os 63 métodos (94 testes total).
- ESLint 8 + `@typescript-eslint` v7 + Prettier 3.
- `tsconfig.eslint.json` para linting type-aware.
- `.gitignore` cobrindo `node_modules/`, `dist/`, `coverage/`, `.env*`.

### Modificado

- 0 `any` em código de produção (eram 13 antes).
- 0 `any[]` em `types.ts` (eram 8 antes).
- `buildSoapEnvelope` extraído para `transport/envelope.ts` (função pura testável).
- `parseSoapFault` extraído para `transport/fault.ts` (parser tolerante).
- `while (true)` em `obterVeiculosJson` substituído por `while (keepPaginating)`.
- `catch (err: any)` substituído por `catch (err)` com `instanceof Error` check.

### Documentação

- README atualizado com 63 métodos, novos erros, SascarClientOptions, exemplos.
- `.env.example` adicionado.
- Este CHANGELOG.

## [1.0.0] — 2024

### Adicionado

- Release inicial com 60 métodos públicos.
- `AsyncQueue` (mutex) para serialização de chamadas.
- README com referência completa.
```

- [ ] **Step 2: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs: add CHANGELOG.md following Keep a Changelog format"
```

---

### Task 3: Atualizar seção "Uso Básico" do README

**Files:**
- Modify: `README.md:1-30`

- [ ] **Step 1: Substituir bloco de uso básico**

oldString em `README.md` (linhas 1-30):
```markdown

# 🚛 Sascar Integra SDK v2.07 - Documentação Completa

SDK corporativo em TypeScript para integração com o Web Service SOAP da **SASCAR (SasIntegra v2.07)**.
Este documento lista **100% dos métodos e atributos** que podem ser consumidos da API através desta biblioteca.

## Instalação

```bash
npm install github:MartielLima/sascar-sdk
# ou
bun add github:MartielLima/sascar-sdk
```

## Uso Básico

```typescript
import { SascarClient } from 'sascar-sdk';
const client = new SascarClient({ usuario: "foo", senha: "bar" });
// Opcional: as credenciais podem ser omitidas se SASCAR_USUARIO e SASCAR_SENHA estiverem no seu .env
```

---
```

newString:
```markdown

# 🚛 Sascar Integra SDK v2.07 — TypeScript

SDK corporativo em TypeScript para integração com o Web Service SOAP da **SASCAR / Michelin ConnectedFleet (SasIntegra v2.07)**.
Este documento lista **100% dos métodos e atributos** que podem ser consumidos da API através desta biblioteca.

> **Status:** SDK auditado em 2026-06-09. Cobertura 100% do manual SasIntegra v2.07 (seções 4.1–4.63). 94 testes, 0 `any` em produção, erros tipados, timeout, retry, transport isolado.

## Instalação

```bash
npm install github:MartielLima/sascar-sdk
# ou
bun add github:MartielLima/sascar-sdk
```

## Uso Básico

```typescript
import { SascarClient } from 'sascar-sdk';

// Opção 1: credenciais explícitas
const client = new SascarClient({ usuario: 'seu_usuario', senha: 'sua_senha' });

// Opção 2: variáveis de ambiente SASCAR_USUARIO e SASCAR_SENHA (recomendado)
const client = new SascarClient();
```

### Opções avançadas (timeout, retry, URL customizada)

```typescript
import { SascarClient } from 'sascar-sdk';

const client = new SascarClient(
  { usuario: 'seu_usuario', senha: 'sua_senha' },
  {
    timeoutMs: 30_000,   // default; timeout por requisição HTTP
    maxRetries: 3,       // default; retry em 5xx e erros de rede
    wsdlUrl: 'https://sasintegra.sascar.com.br/SasIntegra/SasIntegraWSService'
  }
);
```

### Tratamento de erros tipados

```typescript
import {
  SascarApiError,
  SascarAuthError,
  SascarConnectionError,
  SascarRateLimitError,
  SascarTimeoutError
} from 'sascar-sdk';

try {
  const veiculos = await client.obterVeiculos();
} catch (err) {
  if (err instanceof SascarAuthError) {
    console.error('Credenciais inválidas:', err.statusCode);
  } else if (err instanceof SascarRateLimitError) {
    console.error('Rate limit. Aguarde antes de tentar de novo.');
  } else if (err instanceof SascarTimeoutError) {
    console.error(`Timeout após ${err.timeoutMs}ms`);
  } else if (err instanceof SascarConnectionError) {
    console.error('Erro de rede:', err.message);
  } else if (err instanceof SascarApiError) {
    console.error('SOAP Fault:', err.fault?.faultstring, err.fault?.faultcode);
  } else {
    throw err;
  }
}
```

### Variáveis de ambiente

```bash
# .env
SASCAR_USUARIO=seu_usuario
SASCAR_SENHA=sua_senha
```

Veja `.env.example` no repositório.

---
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: update README usage section with options and typed errors"
```

---

### Task 4: Adicionar 3 novos métodos ao README

**Files:**
- Modify: `README.md` (adicionar seções para os 3 novos métodos da F2)

- [ ] **Step 1: Localizar onde adicionar**

Localizar no README a seção `### 🛠️ Cadastros e Entidades` (logo após o último método existente de cadastros — `obterRotas`) e adicionar após ele o `obterEnderecoPosicao`. Localizar a seção `### 📍 Posições e Rastreamento` e adicionar `obterPacotePosicoesRFNacional` + `obterPacotePosicaoMotoristaHistorico` no final.

- [ ] **Step 2: Adicionar `obterEnderecoPosicao` (seção Cadastros)**

Inserir após o bloco `obterRotas`:

```html
<details>
<summary><code><b>obterEnderecoPosicao(latitude: string, longitude: string)</b></code></summary>

**Exemplo de Chamada:**

```typescript
const resultado = await client.obterEnderecoPosicao('-23.5', '-46.6');
console.log(resultado);
```

**Retorno Esperado (`T.EnderecoPosicao[]`):**

```typescript
interface EnderecoPosicao {
  cidade: string;
  Rua: string;
  uf: string;
}
```

</details>
```

- [ ] **Step 3: Adicionar `obterPacotePosicoesRFNacional` e `obterPacotePosicaoMotoristaHistorico` (seção Posições)**

Inserir após o último método da seção Posições:

```html
<details>
<summary><code><b>obterPacotePosicoesRFNacional(quantidade = 3000)</b></code></summary>

**Exemplo de Chamada:**

```typescript
const resultado = await client.obterPacotePosicoesRFNacional(100);
console.log(resultado);
```

**Retorno Esperado (`T.PacotePosicaoXML[]`):** ver tipo `obterPacotePosicoes`.

</details>

<details>
<summary><code><b>obterPacotePosicaoMotoristaHistorico(dataInicio: string, dataFinal: string, idVeiculo?: number)</b></code></summary>

**Exemplo de Chamada:**

```typescript
const resultado = await client.obterPacotePosicaoMotoristaHistorico('2023-10-01', '2023-10-02', 12345);
console.log(resultado);
```

**Retorno Esperado (`T.PacotePosicaoXML[]`):** mesmo tipo de `obterPacotePosicoes`, mas com `idMotorista`, `nomeMotorista` e `estadoLimpadorParabrisa` populados.

</details>
```

- [ ] **Step 4: Rodar `npm run format` para garantir formatação**

Run: `npm run format 2>&1 | tail -3`

- [ ] **Step 5: Validar build/lint**

Run:
```bash
npm run build && npm run lint
```

Expected: ambos verdes.

- [ ] **Step 6: Commit**

```bash
git add README.md
git commit -m "docs: add 3 new methods (F2) to README API reference"
```

---

### Task 5: Atualizar contagem de métodos no README

**Files:**
- Modify: `README.md` (procurar por "60 métodos" e atualizar para 63)

- [ ] **Step 1: Localizar "60" no README**

Run: `grep -n "60 métodos" README.md` (ou similar)

- [ ] **Step 2: Substituir 60 por 63 onde aparecer**

Use `edit` com `replaceAll: true` se houver múltiplas ocorrências. Atualize também o cabeçalho da seção se mencionar "60 métodos".

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: update method count to 63 in README header"
```

---

### Task 6: Validação final + relatório F5

**Files:**
- Create: `docs/audit-report-f5.md`

- [ ] **Step 1: Rodar pipeline completo**

Run:
```bash
npm run build && npm run lint && npm run typecheck && npm test 2>&1 | tail -10
```

Expected: tudo verde, 94/94 testes.

- [ ] **Step 2: Escrever `docs/audit-report-f5.md`**

```markdown
# F5 — Documentação e DX — Relatório de Execução

**Data:** 2026-06-09

## Mudanças aplicadas

- `.env.example` criado com placeholders de credenciais.
- `CHANGELOG.md` criado seguindo Keep a Changelog.
- README.md atualizado:
  - Cabeçalho com status da auditoria (100% manual coverage, 94 testes, 0 any).
  - Seção "Uso Básico" com `SascarClientOptions` (timeoutMs, maxRetries, wsdlUrl).
  - Seção "Tratamento de erros tipados" com 5 classes (`SascarApiError`, `SascarAuthError`, `SascarConnectionError`, `SascarRateLimitError`, `SascarTimeoutError`).
  - Bloco sobre variáveis de ambiente com link para `.env.example`.
  - 3 novos métodos adicionados: `obterEnderecoPosicao`, `obterPacotePosicoesRFNacional`, `obterPacotePosicaoMotoristaHistorico`.
  - Contagem de métodos atualizada de 60 para 63.

## Critérios de aceite

- [x] `.env.example` presente e documentado
- [x] `CHANGELOG.md` segue Keep a Changelog
- [x] README reflete o estado real do código (63 métodos, options, erros)
- [x] Credenciais placeholder (`"foo"`, `"bar"`) removidas
- [x] Build/lint/typecheck/test verdes

## Commits (5)

(Preencher após `git log F4..HEAD`.)

## Estado final do projeto (após F1, F3, F2, F4, F5)

- 63 métodos públicos
- 94 testes (100% statements/functions/lines; ~67% branches em `client.ts` por combinação de opcionais)
- 0 `any` em produção
- 5 classes de erro tipadas
- Camada de transporte isolada (`src/transport/`)
- Timeout + retry com backoff exponencial
- 100% de cobertura do manual SasIntegra v2.07 (seções 4.1–4.63)
- ESLint + Prettier + tsc --noEmit
- CI-ready: `npm run build && npm test && npm run lint && npm run typecheck`

## Auditoria integral concluída

Todas as 5 fases do plano `2026-06-09-sascar-sdk-audit-design.md` foram entregues e aprovadas.
```

- [ ] **Step 3: Commit final**

```bash
git add docs/audit-report-f5.md
git commit -m "docs(audit): F5 completion report (final)"
```

---

## Critérios de aceite globais da F5

- [ ] `npm run build/lint/typecheck/test` verdes
- [ ] `.env.example` presente
- [ ] `CHANGELOG.md` presente
- [ ] README com 63 métodos, opções, erros tipados, sem credenciais placeholder
- [ ] Auditoria integral concluída
